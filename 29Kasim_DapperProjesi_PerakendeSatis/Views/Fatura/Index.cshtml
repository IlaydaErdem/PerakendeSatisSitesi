@model IEnumerable<_29Kasim_DapperProjesi_PerakendeSatis.Models.FaturaModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Yeni", "EY", null, new { @class = "btn btn-warning" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MusteriNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FaturaTarih)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.MusteriNo)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.FaturaTarih)
    </td>
    <td>
        @Html.ActionLink("Düzenle", "EY", new { id = item.FaturaNo }, new { @class = "btn btn-warning" }) |

        @Html.ActionLink("Sil", "Delete", new { id = item.FaturaNo }, new { @class = "btn btn-warning" })
    </td>
</tr>
}

</table>
